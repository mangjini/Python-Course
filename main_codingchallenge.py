def plus (a,b):
  return float(a)+float(b)

p_plus = plus (a=3,b=5)

def minus (a,b):
  return float(a)-float(b)

p_minus = minus ( a= 4,b=6)

def product (a,b):
  return float(a)*float(b)

p_product = product ( a=4,b=6)

def quotient (a,b):
  return float(a) / float(b)

p_quotient = quotient (a=3.5,b=6.7 )

def remainder (a,b):
  return float(a) % float(b)

p_remainder = remainder (a=10,b=6.5)

print(p_plus)
print(p_minus)
print(p_product)
print(p_quotient)
print(p_remainder)

